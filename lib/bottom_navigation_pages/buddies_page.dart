import 'package:flutter/material.dart';

class BuddiesNavBar extends StatelessWidget {
  const BuddiesNavBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Buddies'),
    );
  }
}
